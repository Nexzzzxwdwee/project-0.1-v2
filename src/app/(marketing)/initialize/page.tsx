export default function InitializePage() {
  return (
    <main>
      <h1>Initialize</h1>
      <p>Initialize page placeholder - Phase 2 UI only</p>
      <p>No backend logic, no auth, no API calls.</p>
    </main>
  );
}

